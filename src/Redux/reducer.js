const initialState = {
  coachState: {
    coachExercisePlanState: "",
  },
  userState: {
    user: {
      token: "",
    },
  },
};

export default function reducer(state = initialState, action) {
  return state;
}
